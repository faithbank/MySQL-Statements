# SELECT DISTINCT
SELECT 
    gender
FROM
    employees;
    
# To obtain no duplicate values
SELECT DISTINCT
    gender
FROM
    employees;
 
 
# Task:
# To obtain a list with all different "hire dates" from the "employees" table.
SELECT DISTINCT
    hire_date
FROM
    employees;